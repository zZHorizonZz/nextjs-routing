/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/test', // If this is set then and we got to localhost/test then we get the 404 page, If this is not set then we get the index page
};

export default nextConfig;
